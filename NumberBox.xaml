<UserControl x:Class="ConnectionChecker.NumberBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="33">
    <Border x:Name="Frame" Background="#0C1220"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="3">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="Input" Grid.Column="0"
                     Style="{StaticResource NeonTextBox}"
                     Background="Transparent" BorderThickness="0"
                     FontFamily="Consolas" VerticalContentAlignment="Center"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <RepeatButton Grid.Row="0" Style="{StaticResource SpinButton}"
                              Content="M0,4 L4,0 L8,4" Click="Up_Click"/>
                <RepeatButton Grid.Row="1" Style="{StaticResource SpinButton}"
                              Content="M0,0 L4,4 L8,0" Click="Down_Click"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
